class Solution:
    def isMatch(self, s, p):
        """
        :type s: str
        :type p: str
        :rtype: bool
        """
        while s and p:

        else:
            if not s and not p:
                return True
            else:
                return False
